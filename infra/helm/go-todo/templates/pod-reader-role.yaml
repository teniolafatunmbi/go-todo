kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  namespace: go-todo
  name: pod-reader
rules:
  - apiGroups: ["", "batch"]
    resources: ["pods", "jobs"]
    verbs: ["get", "list", "watch"]
